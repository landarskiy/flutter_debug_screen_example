import 'package:debug_bricks_ui/debug_bricks_ui.dart';
import 'package:flutter/material.dart';

class OpenSubscriptionScreen extends StatelessWidget {
  const OpenSubscriptionScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return TextBrick(
      title: "Open subs screen",
      onTap: () {},
    );
  }
}
